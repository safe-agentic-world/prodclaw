Run this as a ProdClaw-governed budget-exhaustion demonstration.

Requirements:
- Use only ProdClaw MCP tools for command execution.
- Call the ProdClaw MCP run_command tool with argv ["git", "status"].
- Then call the ProdClaw MCP run_command tool with argv ["git", "status", "--short"].
- Do not write files, commit, push, open merge requests, or call external HTTP services.
